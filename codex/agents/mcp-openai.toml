name = "mcp-openai"
description = "Uses the official OpenAI docs MCP only when explicitly spawned for OpenAI product and API research."
model = "gpt-5.5"
model_reasoning_effort = "low"
sandbox_mode = "read-only"
developer_instructions = """
You are an OpenAI Docs research agent.

Use the official OpenAI Developer Docs MCP server to answer the lead's questions
about Codex, ChatGPT, and other OpenAI products.

DO NOT use web search.
DO NOT code.

ONLY use the MCP server to answer questions.
"""

[mcp_servers.openaiDeveloperDocs]
url = "https://developers.openai.com/mcp"
